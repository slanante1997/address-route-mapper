# Copy to `.env` and paste your key for local development with `netlify dev`.
# In production, set this same variable in the Netlify dashboard
# (Site settings → Environment variables) instead.
# Get a key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
